@page "/"
@layout PageLayout

<Title>Will's Tools</Title>

<div class="text-center">
    Be adventurous. Click some buttons.
</div>

@code {
    [CascadingParameter(Name = "PageLayout")] PageLayout Layout { get; set; }

    protected override void OnInitialized()
    {
        base.OnInitialized();
        if (Layout == null) return;
        Layout.Reset();
    }
}